# PR Flow Smoke Test

This temporary file verifies that public contributions can run through a branch, pull request and validator workflow.

Expected outcome:

- pull request can be opened
- `validate` workflow runs
- branch protection remains active
- no merge happens without human review

